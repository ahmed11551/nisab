# 🚀 Быстрый старт локально

## 📍 Важные URL

### Backend API (порт 3000)
- **Health check:** http://localhost:3000/health
- **API endpoints:** http://localhost:3000/api/v1/*

⚠️ **Важно:** Backend — это API сервер, на корневом пути `/` нет маршрута. Это нормально!

### Frontend (порт 5173)
- **Главная страница:** http://localhost:5173
- **Telegram Mini App:** http://localhost:5173 (откройте в Telegram)

## 🏃 Запуск проекта

### Вариант 1: Отдельные терминалы (рекомендуется)

**Терминал 1 - Backend:**
```bash
cd backend
npm run dev
```

**Терминал 2 - Frontend:**
```bash
cd frontend
npm run dev
```

### Вариант 2: Вместе (из корня)

```bash
npm run dev
```

## ✅ Проверка работы

1. **Backend API:**
   - Откройте: http://localhost:3000/health
   - Должен вернуться: `{"status":"ok","timestamp":"..."}`

2. **Frontend:**
   - Откройте: http://localhost:5173
   - Должна загрузиться главная страница приложения

## 🔍 Доступные API endpoints

- `GET /health` - Проверка работы сервера
- `GET /api/v1/funds` - Список фондов
- `GET /api/v1/campaigns` - Список кампаний
- `POST /api/v1/donations/init` - Инициализация пожертвования
- `POST /api/v1/subscriptions/init` - Инициализация подписки
- `POST /api/v1/zakat/calc` - Расчет закята
- И другие...

Подробная документация: [docs/API.md](./docs/API.md)

## ⚠️ Важные замечания

1. **Backend (`localhost:3000`) - это API сервер:**
   - На корневом пути `/` нет страницы (это нормально!)
   - Используйте `/health` для проверки
   - Все API endpoints начинаются с `/api/v1/`

2. **Frontend (`localhost:5173`) - это веб-приложение:**
   - Главная страница: http://localhost:5173
   - Frontend делает запросы к Backend API

3. **Telegram Mini App:**
   - Для работы в Telegram нужен домен (не localhost)
   - Для разработки можно использовать ngrok или локальный тест

## 🐛 Проблемы?

### Backend не запускается:
- Проверьте, что PostgreSQL запущен: `docker ps | grep nisab_postgres`
- Проверьте `backend/.env` - правильный ли `DATABASE_URL`

### Frontend не запускается:
- Проверьте, что порт 5173 свободен
- Проверьте `frontend/.env` - правильный ли `VITE_API_URL`

### Port already in use:
- Остановите процессы: `Get-Process -Name node | Stop-Process -Force`
- Или измените порты в конфигурации

