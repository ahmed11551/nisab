# 🤖 Полная инструкция по созданию и настройке Telegram бота

## 📋 Содержание

1. [Создание бота через @BotFather](#1-создание-бота-через-botfather)
2. [Создание Mini App](#2-создание-mini-app)
3. [Настройка проекта](#3-настройка-проекта)
4. [Настройка базы данных](#4-настройка-базы-данных)
5. [Запуск backend](#5-запуск-backend)
6. [Настройка webhook](#6-настройка-webhook)
7. [Тестирование бота](#7-тестирование-бота)
8. [Развертывание в продакшн](#8-развертывание-в-продакшн)
9. [Решение проблем](#9-решение-проблем)

---

## 1. Создание бота через @BotFather

### Шаг 1.1: Найти @BotFather

1. Откройте Telegram (на телефоне или в браузере)
2. Найдите бота **@BotFather** в поиске
3. Нажмите **Start** для начала работы

### Шаг 1.2: Создать нового бота

1. Отправьте команду: `/newbot`
2. @BotFather попросит ввести **название бота**:
   ```
   Nisab - Платформа для садака и закята
   ```
3. Затем попросит ввести **username бота** (должен заканчиваться на `_bot`):
   ```
   nisab_platform_bot
   ```
   ⚠️ **Важно:** Username должен быть уникальным и заканчиваться на `_bot`
   
4. Если username занят, попробуйте другие варианты:
   - `nisab_charity_bot`
   - `nisab_app_bot`
   - `nisab_sadaqa_bot`

### Шаг 1.3: Сохранить токен

После успешного создания бота, @BotFather отправит вам сообщение:

```
Done! Congratulations on your new bot. You will find it at t.me/your_bot_username.

Use this token to access the HTTP API:
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

Keep your token secure and store it safely, it can be used by anyone to control your bot.
```

📝 **СОХРАНИТЕ ЭТОТ ТОКЕН!** Он понадобится для настройки backend.

**Пример токена:** `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`

---

## 2. Создание Mini App

### Шаг 2.1: Создать Mini App

1. В @BotFather выберите созданный бот (если бот не выбран, отправьте `/mybots` и выберите ваш бот)
2. Отправьте команду: `/newapp`
3. Выберите созданный бот из списка

### Шаг 2.2: Настроить Mini App

@BotFather попросит ввести:

1. **Название приложения:**
   ```
   Nisab
   ```

2. **Короткое описание:**
   ```
   Платформа для садака и закята
   ```

3. **URL (для продакшн):**
   ```
   https://your-domain.com
   ```
   Или для разработки:
   ```
   http://localhost:5173
   ```

4. **Аватар (опционально):** Можете загрузить изображение или пропустить

5. **GIF анимация (опционально):** Можете пропустить

### Шаг 2.3: Сохранить WebApp Secret

После создания Mini App, @BotFather отправит вам:
- Web App URL
- **WebApp Secret** — это важно!

📝 **СОХРАНИТЕ WebApp Secret!** Он нужен для проверки подлинности запросов.

**Пример WebApp Secret:** `your_webapp_secret_string_here`

---

## 3. Настройка проекта

### Шаг 3.1: Создать файл `.env` в backend

Создайте файл `backend/.env` со следующим содержимым:

```env
# Environment
NODE_ENV=development
PORT=3000

# Database
DATABASE_URL=postgresql://nisab:nisab_password@localhost:5432/nisab_db

# Telegram Bot
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_WEBAPP_SECRET=your_webapp_secret_here

# Frontend URL
CORS_ORIGIN=http://localhost:5173
FRONTEND_URL=http://localhost:5173

# JWT
JWT_SECRET=your_jwt_secret_change_in_production

# Payments (можно настроить позже)
YOOKASSA_SHOP_ID=
YOOKASSA_SECRET_KEY=
CLOUDPAYMENTS_PUBLIC_ID=
CLOUDPAYMENTS_SECRET_KEY=
```

⚠️ **Замените:**
- `your_bot_token_here` на токен из @BotFather
- `your_webapp_secret_here` на WebApp Secret
- `your_jwt_secret_change_in_production` на случайную строку (минимум 32 символа)

### Шаг 3.2: Создать файл `.env` в frontend

Создайте файл `frontend/.env`:

```env
VITE_API_URL=http://localhost:3000
VITE_APP_NAME=Nisab
VITE_TELEGRAM_BOT_NAME=your_bot_username_bot
```

⚠️ **Замените:**
- `your_bot_username_bot` на username вашего бота (без `@`)

---

## 4. Настройка базы данных

### Шаг 4.1: Установить PostgreSQL

**Windows:**
1. Скачайте PostgreSQL с официального сайта: https://www.postgresql.org/download/windows/
2. Установите PostgreSQL
3. Запомните пароль пользователя `postgres`

**Или используйте Docker:**
```bash
docker-compose up -d postgres
```

### Шаг 4.2: Создать базу данных

**Вариант A: Через psql (командная строка)**

```bash
# Подключиться к PostgreSQL
psql -U postgres

# Создать базу данных
CREATE DATABASE nisab_db;

# Создать пользователя
CREATE USER nisab WITH PASSWORD 'nisab_password';

# Дать права пользователю
GRANT ALL PRIVILEGES ON DATABASE nisab_db TO nisab;

# Выйти
\q
```

**Вариант B: Через pgAdmin (графический интерфейс)**

1. Откройте pgAdmin
2. Создайте базу данных: правый клик на "Databases" → "Create" → "Database"
3. Имя: `nisab_db`
4. Создайте пользователя: правый клик на "Login/Group Roles" → "Create" → "Login/Group Role"
5. Имя: `nisab`, пароль: `nisab_password`
6. На вкладке "Privileges" дайте все права на базу данных

### Шаг 4.3: Запустить миграции

```bash
cd backend
npm run migrate
```

Должно вывести:
```
✓ Migration completed successfully
```

### Шаг 4.4: (Опционально) Заполнить тестовыми данными

```bash
cd backend
npm run seed
```

---

## 5. Запуск backend

### Шаг 5.1: Установить зависимости (если еще не установлены)

```bash
cd backend
npm install
```

### Шаг 5.2: Запустить backend

```bash
npm run dev
```

Вы должны увидеть:
```
Server running on port 3000
Environment: development
Health check: http://localhost:3000/health
API docs: http://localhost:3000/api/v1
✓ Database connection: OK
✓ Telegram Bot Token: OK
```

### Шаг 5.3: Проверить работу backend

Откройте в браузере: http://localhost:3000/health

Должен вернуться JSON:
```json
{
  "status": "ok",
  "timestamp": "2025-01-01T12:00:00.000Z"
}
```

---

## 6. Настройка webhook

### Для локальной разработки (с ngrok)

#### Шаг 6.1: Установить и запустить ngrok

1. Скачайте ngrok: https://ngrok.com/download
2. Распакуйте и запустите:
   ```bash
   ngrok http 3000
   ```
3. Скопируйте HTTPS URL (например: `https://abc123.ngrok.io`)

#### Шаг 6.2: Установить webhook

**Вариант A: Через API (POST)**

```bash
curl -X POST http://localhost:3000/api/v1/telegram/set-webhook \
  -H "Content-Type: application/json" \
  -d '{"url": "https://abc123.ngrok.io/api/v1/telegram/webhook"}'
```

**Вариант B: Через GET запрос**

```bash
curl "http://localhost:3000/api/v1/telegram/set-webhook?url=https://abc123.ngrok.io/api/v1/telegram/webhook"
```

**Вариант C: Напрямую через Telegram API**

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -d "url=https://abc123.ngrok.io/api/v1/telegram/webhook"
```

⚠️ **Замените:**
- `<YOUR_BOT_TOKEN>` на токен вашего бота
- `abc123.ngrok.io` на ваш ngrok URL

#### Шаг 6.3: Проверить webhook

```bash
curl http://localhost:3000/api/v1/telegram/webhook-info
```

Или через Telegram API:
```bash
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo"
```

Должен вернуться ответ с информацией о webhook:
```json
{
  "ok": true,
  "result": {
    "url": "https://abc123.ngrok.io/api/v1/telegram/webhook",
    "has_custom_certificate": false,
    "pending_update_count": 0
  }
}
```

### Для продакшн

Замените ngrok URL на ваш реальный домен:
```bash
curl -X POST http://localhost:3000/api/v1/telegram/set-webhook \
  -H "Content-Type: application/json" \
  -d '{"url": "https://your-domain.com/api/v1/telegram/webhook"}'
```

⚠️ **Важно:**
- URL должен начинаться с `https://` (не `http://`)
- URL должен быть доступен из интернета
- SSL сертификат должен быть валидным

---

## 7. Тестирование бота

### Шаг 7.1: Найти бота в Telegram

1. Откройте Telegram
2. Найдите вашего бота по username (например: `@nisab_platform_bot`)
3. Нажмите **Start**

### Шаг 7.2: Проверить команды

Отправьте боту команды и проверьте ответы:

- `/start` - Должно показать главное меню с кнопками
- `/help` - Должно показать справку
- `/info` - Должно показать информацию о боте
- `/donate` - Должно показать меню пожертвований

### Шаг 7.3: Проверить кнопки

1. Нажмите на кнопку "💰 Пожертвовать"
2. Нажмите на кнопку "🌐 Открыть Mini App"
3. Все должно работать корректно

### Шаг 7.4: Проверить сохранение пользователя

После отправки команды `/start`, пользователь должен автоматически сохраниться в базу данных.

Проверьте в базе данных:
```sql
SELECT * FROM users ORDER BY created_at DESC LIMIT 1;
```

Должен вернуться ваш пользователь с `tg_id` вашего Telegram аккаунта.

---

## 8. Развертывание в продакшн

### Шаг 8.1: Настроить переменные окружения

Обновите `backend/.env` для продакшн:

```env
NODE_ENV=production
PORT=3000

DATABASE_URL=postgresql://user:password@host:5432/nisab_db

TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_WEBAPP_SECRET=your_webapp_secret_here

CORS_ORIGIN=https://your-domain.com
FRONTEND_URL=https://your-domain.com

JWT_SECRET=your_secure_jwt_secret_here_min_32_chars

# Настройте платежи
YOOKASSA_SHOP_ID=your_shop_id
YOOKASSA_SECRET_KEY=your_secret_key
CLOUDPAYMENTS_PUBLIC_ID=your_public_id
CLOUDPAYMENTS_SECRET_KEY=your_secret_key
```

### Шаг 8.2: Обновить Mini App URL

1. В @BotFather отправьте `/mybots`
2. Выберите ваш бот
3. Выберите "Edit App"
4. Измените URL на: `https://your-domain.com`

### Шаг 8.3: Установить webhook

```bash
curl -X POST https://your-domain.com/api/v1/telegram/set-webhook \
  -H "Content-Type: application/json" \
  -d '{"url": "https://your-domain.com/api/v1/telegram/webhook"}'
```

### Шаг 8.4: Проверить webhook

```bash
curl https://your-domain.com/api/v1/telegram/webhook-info
```

---

## 9. Решение проблем

### Проблема: Бот не отвечает

**Проверки:**

1. ✅ Backend запущен?
   ```bash
   curl http://localhost:3000/health
   ```

2. ✅ Webhook установлен?
   ```bash
   curl http://localhost:3000/api/v1/telegram/webhook-info
   ```

3. ✅ Токен правильный?
   - Проверьте `backend/.env`: `TELEGRAM_BOT_TOKEN`
   - Проверьте логи backend на ошибки

4. ✅ Webhook URL доступен?
   - Для localhost используйте ngrok
   - Для продакшн проверьте, что домен работает

**Решение:**

```bash
# Перезапустить backend
cd backend
npm run dev

# Проверить логи на ошибки
# Посмотрите вывод консоли
```

---

### Проблема: Webhook не устанавливается

**Причины:**

1. URL недоступен из интернета (localhost без ngrok)
2. URL не использует HTTPS
3. Неверный токен бота
4. SSL сертификат невалидный

**Решение:**

1. Используйте ngrok для локальной разработки:
   ```bash
   ngrok http 3000
   ```

2. Для продакшн проверьте SSL:
   ```bash
   curl https://your-domain.com/api/v1/telegram/webhook
   ```

3. Проверьте токен:
   ```bash
   # Тест через Telegram API
   curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getMe"
   ```

---

### Проблема: Пользователи не сохраняются

**Проверки:**

1. ✅ База данных подключена?
   ```bash
   # Проверьте логи backend
   # Должно быть: "✓ Database connection: OK"
   ```

2. ✅ Миграции выполнены?
   ```bash
   cd backend
   npm run migrate
   ```

3. ✅ Таблица `users` существует?
   ```sql
   \dt users
   ```

**Решение:**

```bash
# Перезапустить миграции
cd backend
npm run migrate

# Проверить логи backend на ошибки
```

---

### Проблема: Mini App не открывается

**Проверки:**

1. ✅ Frontend запущен?
   - Должен быть доступен по URL из Mini App

2. ✅ URL правильный?
   - Проверьте URL в настройках Mini App
   - Для разработки: `http://localhost:5173`
   - Для продакшн: `https://your-domain.com`

3. ✅ CORS настроен?
   - Проверьте `backend/.env`: `CORS_ORIGIN`

**Решение:**

1. Обновите URL в @BotFather: `/mybots` → выберите бот → "Edit App"

2. Проверьте `backend/.env`:
   ```env
   CORS_ORIGIN=https://your-domain.com
   ```

---

## 📚 Дополнительные ресурсы

- [Telegram Bot API Documentation](https://core.telegram.org/bots/api)
- [Telegram WebApp Documentation](https://core.telegram.org/bots/webapps)
- [ngrok Documentation](https://ngrok.com/docs)
- [PostgreSQL Documentation](https://www.postgresql.org/docs/)

---

## ✅ Чек-лист настройки

- [ ] Бот создан через @BotFather
- [ ] Токен бота сохранен в `backend/.env`
- [ ] Mini App создан через @BotFather
- [ ] WebApp Secret сохранен в `backend/.env`
- [ ] База данных создана и настроена
- [ ] Миграции выполнены
- [ ] Backend запущен и работает
- [ ] Webhook установлен
- [ ] Бот отвечает на команды
- [ ] Пользователи сохраняются в БД
- [ ] Mini App открывается
- [ ] Все команды работают

---

## 🎉 Готово!

После выполнения всех шагов ваш бот должен полностью работать!

Если возникли проблемы, проверьте раздел [Решение проблем](#9-решение-проблем) или логи backend.
