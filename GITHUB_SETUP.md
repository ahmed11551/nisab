# Инструкция по загрузке проекта в GitHub и развертыванию на Vercel

## Шаг 1: Инициализация Git репозитория

### 1.1. Инициализация Git

```bash
# В корневой директории проекта
git init

# Добавьте все файлы
git add .

# Первый коммит
git commit -m "Initial commit: Nisab Sadaqa Pass project"
```

### 1.2. Создание .gitignore (уже создан)

Убедитесь, что `.gitignore` содержит все необходимые исключения:
- `node_modules/`
- `.env` файлы
- `dist/` и `build/`
- Логи и временные файлы

## Шаг 2: Создание репозитория на GitHub

### 2.1. Через веб-интерфейс

1. Откройте https://github.com
2. Нажмите кнопку "New repository" (или "+" → "New repository")
3. Заполните форму:
   - **Repository name:** `nisab-sadaqa` (или ваше имя)
   - **Description:** "Telegram Mini App for Sadaqa donations, subscriptions, and Zakat calculator"
   - **Visibility:** Private (рекомендуется) или Public
   - НЕ создавайте README, .gitignore или лицензию (они уже есть)
4. Нажмите "Create repository"

### 2.2. Подключение локального репозитория к GitHub

```bash
# Добавьте remote (замените YOUR_USERNAME и REPOSITORY_NAME)
git remote add origin https://github.com/YOUR_USERNAME/nisab-sadaqa.git

# Или используйте SSH:
# git remote add origin git@github.com:YOUR_USERNAME/nisab-sadaqa.git

# Проверьте remote
git remote -v

# Установите главную ветку (если нужно)
git branch -M main

# Загрузите код в GitHub
git push -u origin main
```

## Шаг 3: Настройка переменных окружения в GitHub

### 3.1. GitHub Secrets (для CI/CD)

1. Перейдите в Settings → Secrets and variables → Actions
2. Добавьте следующие секреты (если планируете использовать GitHub Actions):
   - `DATABASE_URL`
   - `TELEGRAM_BOT_TOKEN`
   - `TELEGRAM_WEBAPP_SECRET`
   - `YOOKASSA_SHOP_ID`
   - `YOOKASSA_SECRET_KEY`
   - `CLOUDPAYMENTS_PUBLIC_ID`
   - `CLOUDPAYMENTS_SECRET_KEY`

## Шаг 4: Развертывание Frontend на Vercel

### 4.1. Через Vercel Dashboard

1. Откройте https://vercel.com
2. Нажмите "Add New..." → "Project"
3. Импортируйте репозиторий из GitHub:
   - Выберите ваш репозиторий `nisab-sadaqa`
   - Vercel автоматически определит проект

4. Настройте проект:
   - **Framework Preset:** Vite
   - **Root Directory:** `frontend`
   - **Build Command:** `npm run build`
   - **Output Directory:** `dist`
   - **Install Command:** `npm install`

5. Настройте переменные окружения:
   ```
   VITE_API_URL=https://your-backend-domain.com
   VITE_APP_NAME=Nisab
   VITE_TELEGRAM_BOT_NAME=your_bot_name
   ```

6. Нажмите "Deploy"

### 4.2. Через Vercel CLI (альтернатива)

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите в Vercel
vercel login

# Перейдите в директорию frontend
cd frontend

# Задеплойте проект
vercel

# Следуйте инструкциям в CLI
```

## Шаг 5: Развертывание Backend

### Вариант A: Отдельный сервер (рекомендуется для production)

Backend лучше развернуть на отдельном сервере (Railway, Render, DigitalOcean и т.д.) из-за:
- Долгих соединений с базой данных
- Webhooks от платежных систем
- Фоновых задач

#### Пример: Railway

1. Перейдите на https://railway.app
2. Создайте новый проект
3. Подключите GitHub репозиторий
4. Выберите директорию `backend`
5. Добавьте PostgreSQL и переменные окружения
6. Деплой автоматический при push

### Вариант B: Vercel Serverless Functions

Можно развернуть backend через Vercel, но потребуются изменения:

1. Создайте отдельный проект на Vercel для backend
2. Настройте как Node.js проект
3. Root Directory: `backend`
4. Build Command: `npm run build`
5. Output Directory: `dist`

**Примечание:** Webhooks могут работать с задержкой из-за cold start в serverless функциях.

## Шаг 6: Настройка доменов и переменных окружения

### 6.1. Переменные окружения на Vercel (Frontend)

В настройках проекта на Vercel:

**Environment Variables:**
```
VITE_API_URL=https://your-backend-domain.com
VITE_APP_NAME=Nisab
VITE_TELEGRAM_BOT_NAME=your_bot_name
```

### 6.2. Переменные окружения на Backend сервере

Все переменные из `backend/.env.example` нужно добавить в настройки сервера.

## Шаг 7: Обновление конфигурации для Vercel

### 7.1. Обновите frontend/.env для production

В Vercel добавьте переменные окружения вместо файла `.env`.

### 7.2. Обновите API URL в коде (если нужно)

После развертывания backend получите его URL и обновите `VITE_API_URL` в Vercel.

## Шаг 8: Настройка Telegram Bot Webhook

После развертывания backend:

```bash
# Замените YOUR_BOT_TOKEN и YOUR_BACKEND_URL
curl -X POST "https://api.telegram.org/botYOUR_BOT_TOKEN/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://YOUR_BACKEND_URL/api/v1/telegram/webhook"}'
```

## Шаг 9: Настройка платежных webhooks

### 9.1. YooKassa

1. В личном кабинете YooKassa
2. Настройки магазина → Webhooks
3. Добавьте URL: `https://YOUR_BACKEND_URL/api/v1/payments/webhook/yookassa`

### 9.2. CloudPayments

1. В личном кабинете CloudPayments
2. Настройки → Webhooks
3. Добавьте URL: `https://YOUR_BACKEND_URL/api/v1/payments/webhook/cloudpayments`

## Шаг 10: Проверка работы

### 10.1. Проверка Frontend

1. Откройте URL вашего Vercel проекта
2. Проверьте, что страницы загружаются
3. Проверьте, что API запросы идут на правильный backend URL

### 10.2. Проверка Backend

```bash
curl https://YOUR_BACKEND_URL/health
# Должен вернуть: {"status":"ok","timestamp":"..."}
```

### 10.3. Проверка Telegram Bot

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Бот должен ответить

## 📝 Чеклист развертывания

- [ ] Репозиторий создан на GitHub
- [ ] Код загружен в GitHub
- [ ] Frontend развернут на Vercel
- [ ] Backend развернут (Railway/Render/другой сервис)
- [ ] Переменные окружения настроены на обоих сервисах
- [ ] Telegram Bot webhook настроен
- [ ] Платежные webhooks настроены
- [ ] Домены настроены (если нужно)
- [ ] Проверена работа frontend
- [ ] Проверена работа backend
- [ ] Проверена работа Telegram Bot

## 🔧 Полезные команды

```bash
# Добавить изменения в Git
git add .

# Создать коммит
git commit -m "Описание изменений"

# Отправить в GitHub
git push origin main

# Проверить статус
git status

# Просмотреть историю
git log --oneline
```

## ⚠️ Важные замечания

1. **Не загружайте `.env` файлы в GitHub** - они уже в `.gitignore`
2. **Используйте GitHub Secrets** для CI/CD переменных
3. **Backend лучше на отдельном сервере** из-за особенностей webhooks и БД
4. **Всегда используйте HTTPS** для production
5. **Настройте мониторинг** после развертывания

## 🆘 Проблемы?

- **Vercel не может собрать проект:** Проверьте Build Command и Output Directory
- **API запросы не работают:** Проверьте `VITE_API_URL` и CORS настройки
- **Webhooks не работают:** Проверьте, что backend доступен извне и URL правильный
- **Telegram Bot не отвечает:** Проверьте webhook URL и токен бота

## ✅ Готово!

После выполнения всех шагов ваш проект будет развернут на Vercel и готов к использованию!

