# Nisab - Садака Пасс

Telegram Mini App для пожертвований (садака), регулярных подписок (садака-джария), калькулятора закята и целевых кампаний.

## 🎯 Основные функции

- **Пожертвования**: Единоразовые пожертвования по целям (мечеть, сироты, проекты)
- **Подписки**: Регулярная милостыня (садака-джария) с автоматическими отчислениями
- **Калькулятор закята**: Расчёт и выплата закята
- **Целевые кампании**: Создание и участие в краудфандинговых кампаниях
- **Фонды-партнёры**: Каталог фондов и заявки на партнёрство

## 🏗️ Архитектура

```
nisab/
├── frontend/          # Telegram Mini App (React/TypeScript)
├── backend/           # API Server (Node.js/TypeScript)
├── shared/            # Общие типы и утилиты
└── docs/              # Документация
```

## 🚀 Быстрый старт

### Предварительные требования

- Node.js >= 18.0.0
- npm >= 9.0.0
- PostgreSQL >= 14
- Redis (опционально, для кеширования)
- Elasticsearch (опционально, для поиска фондов)

### Установка

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/ahmed11551/nisab.git
cd nisab
```

2. **Установите зависимости:**
```bash
npm run install:all
```

3. **Создайте файлы .env:**

   **backend/.env:**
   ```env
   DATABASE_URL=postgresql://nisab:nisab_password@localhost:5432/nisab_db
   TELEGRAM_BOT_TOKEN=your_bot_token_here
   TELEGRAM_WEBAPP_SECRET=your_webapp_secret_here
   CORS_ORIGIN=http://localhost:5173
   API_TOKEN=test_token_123
   BOT_E_REPLIKA_URL=https://bot.e-replika.ru
   ```

   **frontend/.env:**
   ```env
   VITE_API_URL=http://localhost:3000
   ```

4. **Запустите PostgreSQL:**
```bash
# Через Docker (рекомендуется)
docker run -d -p 5432:5432 -e POSTGRES_PASSWORD=nisab_password -e POSTGRES_DB=nisab_db postgres

# Или локально
createdb nisab_db
```

5. **Запустите миграции:**
```bash
cd backend
npm run migrate
```

6. **Запустите проект:**
```bash
npm run dev
```

7. **Проверьте работу:**
- Frontend: http://localhost:5173
- Backend: http://localhost:3000/health

**📖 Полная инструкция:** См. [START_HERE.md](./START_HERE.md) для начала работы.

**🎯 Следующие шаги:** См. [NEXT_STEPS.md](./NEXT_STEPS.md) для деплоя и настройки production.

## 📁 Структура проекта

### Frontend (`/frontend`)

Telegram Mini App на React + TypeScript с использованием Telegram WebApp SDK.

- `src/pages/` - страницы приложения
- `src/components/` - переиспользуемые компоненты
- `src/services/` - API клиенты и интеграции
- `src/store/` - управление состоянием (Zustand)
- `src/utils/` - утилиты

### Backend (`/backend`)

REST API на Node.js + Express + TypeScript.

- `src/routes/` - маршруты API
- `src/controllers/` - контроллеры
- `src/services/` - бизнес-логика
- `src/models/` - модели базы данных
- `src/middleware/` - промежуточное ПО
- `src/integrations/` - интеграции с внешними сервисами

## 🔌 API Эндпоинты

Основные группы эндпоинтов:

- `/api/v1/donations/*` - пожертвования
- `/api/v1/subscriptions/*` - подписки
- `/api/v1/zakat/*` - калькулятор закята
- `/api/v1/campaigns/*` - целевые кампании
- `/api/v1/partners/*` - фонды-партнёры
- `/api/v1/funds/*` - фонды
- `/api/v1/me/*` - профиль и история

Подробная документация API: [docs/API.md](./docs/API.md)

## 🔐 Безопасность

- Валидация Telegram WebApp initData
- JWT токены для аутентификации
- Подписи webhooks от платежных провайдеров
- Rate limiting
- CORS настройки

## 📦 Развёртывание

- **GitHub:** См. [GIT_INSTRUCTIONS.md](./GIT_INSTRUCTIONS.md) для загрузки в GitHub
- **Vercel:** См. [DEPLOY_VERCEL.md](./DEPLOY_VERCEL.md) для развертывания на Vercel
- **Детально:** См. [GITHUB_SETUP.md](./GITHUB_SETUP.md) для полной инструкции
- **Чеклист:** См. [DEPLOYMENT_CHECKLIST.md](./DEPLOYMENT_CHECKLIST.md)
- **Быстро:** См. [README_DEPLOY.md](./README_DEPLOY.md)

## 📝 Лицензия

Проприетарная

## 👥 Контакты

Для вопросов и поддержки обращайтесь к команде разработки.

