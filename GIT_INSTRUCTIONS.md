# Инструкция по загрузке в GitHub

## 🚀 Быстрая инструкция

### Шаг 1: Инициализация Git (если еще не сделано)

```bash
# Проверьте, есть ли уже Git репозиторий
git status

# Если ошибка "not a git repository", инициализируйте:
git init
```

### Шаг 2: Добавьте все файлы

```bash
# Добавьте все файлы в staging
git add .

# Проверьте статус
git status
```

### Шаг 3: Создайте первый коммит

```bash
git commit -m "Initial commit: Nisab Sadaqa Pass - Telegram Mini App for donations, subscriptions, and Zakat calculator"
```

### Шаг 4: Создайте репозиторий на GitHub

1. Откройте https://github.com/new
2. Заполните форму:
   - **Repository name:** `nisab-sadaqa`
   - **Description:** "Telegram Mini App for Sadaqa donations, subscriptions, and Zakat calculator"
   - **Visibility:** Private (рекомендуется) или Public
   - НЕ создавайте README, .gitignore, или лицензию (они уже есть)

3. Нажмите **"Create repository"**

### Шаг 5: Подключите локальный репозиторий к GitHub

```bash
# Добавьте remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/nisab-sadaqa.git

# Или используйте SSH (если настроен):
# git remote add origin git@github.com:YOUR_USERNAME/nisab-sadaqa.git

# Проверьте remote
git remote -v
```

### Шаг 6: Отправьте код в GitHub

```bash
# Установите главную ветку
git branch -M main

# Отправьте код
git push -u origin main
```

### Шаг 7: Проверка

Откройте https://github.com/YOUR_USERNAME/nisab-sadaqa - вы должны увидеть все файлы проекта.

## 📝 Полезные команды Git

```bash
# Просмотр статуса
git status

# Добавить изменения
git add .

# Создать коммит
git commit -m "Описание изменений"

# Отправить изменения
git push origin main

# Получить изменения
git pull origin main

# Просмотр истории
git log --oneline

# Просмотр изменений
git diff
```

## 🔄 Рабочий процесс

### Обычный workflow:

```bash
# 1. Проверить статус
git status

# 2. Добавить изменения
git add .

# 3. Создать коммит
git commit -m "Описание изменений"

# 4. Отправить в GitHub
git push origin main
```

## ⚠️ Важно

1. **НЕ коммитьте `.env` файлы** - они уже в `.gitignore`
2. **Коммитьте часто** - маленькие коммиты лучше больших
3. **Используйте понятные сообщения коммитов**

## ✅ Готово!

Ваш код теперь в GitHub и готов к развертыванию на Vercel!

Следующий шаг: см. [DEPLOY_VERCEL.md](./DEPLOY_VERCEL.md) для развертывания на Vercel.

