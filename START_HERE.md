# 🚀 Начните отсюда!

## Быстрая проверка проекта

Проект Nisab готов к запуску. Следуйте этим шагам по порядку:

## Шаг 1: Проверка зависимостей

```bash
# Проверьте версию Node.js
node --version  # Должна быть >= 18.0.0

# Проверьте версию npm
npm --version   # Должна быть >= 9.0.0

# Установите зависимости
npm run install:all
```

## Шаг 2: Настройка базы данных

### Вариант A: Docker (рекомендуется)

```bash
# Запустите PostgreSQL, Redis, Elasticsearch
docker-compose up -d postgres redis elasticsearch

# Подождите 10-15 секунд, пока сервисы запустятся
```

### Вариант B: Локально

```bash
# Установите PostgreSQL и создайте базу данных
createdb nisab_db
```

## Шаг 3: Создание файлов окружения

```bash
# Backend
cd backend
# Создайте файл .env на основе примера (если есть)
# Или создайте вручную с настройками из SETUP_GUIDE.md

# Frontend
cd ../frontend
# Создайте файл .env с настройками:
# VITE_API_URL=http://localhost:3000
# VITE_APP_NAME=Nisab
# VITE_TELEGRAM_BOT_NAME=your_bot_name
```

**Минимальные настройки для начала:**

### backend/.env
```env
NODE_ENV=development
PORT=3000
DATABASE_URL=postgresql://nisab:nisab_password@localhost:5432/nisab_db
TELEGRAM_BOT_TOKEN=your_token_here
TELEGRAM_WEBAPP_SECRET=your_secret_here
ELASTICSEARCH_URL=http://localhost:9200
API_TOKEN=test_token_123
```

### frontend/.env
```env
VITE_API_URL=http://localhost:3000
VITE_APP_NAME=Nisab
```

## Шаг 4: Запуск миграций

```bash
cd backend
npm run migrate
npm run seed  # Для тестовых данных (опционально)
```

## Шаг 5: Запуск проекта

```bash
# Вернитесь в корневую директорию
cd ..

# Запустите frontend и backend
npm run dev
```

## Шаг 6: Проверка работы

1. **Backend Health Check:**
   ```bash
   curl http://localhost:3000/health
   ```
   Должен вернуть: `{"status":"ok","timestamp":"..."}`

2. **Frontend:**
   - Откройте http://localhost:5173 в браузере
   - Должна загрузиться главная страница

3. **Telegram Bot:**
   - Найдите своего бота в Telegram
   - Отправьте `/start`
   - Бот должен ответить

## 📚 Полезные ссылки

- **Подробная инструкция:** [SETUP_GUIDE.md](./SETUP_GUIDE.md)
- **Быстрый старт:** [QUICKSTART.md](./QUICKSTART.md)
- **API документация:** [docs/API.md](./docs/API.md)
- **Развертывание:** [docs/DEPLOYMENT.md](./docs/DEPLOYMENT.md)
- **Чеклист:** [CHECKLIST.md](./CHECKLIST.md)

## ⚠️ Важные замечания

1. **Telegram Bot Token** - обязательно для работы с ботом
2. **Платежные системы** - нужны для реальных платежей (можно отложить для тестирования)
3. **Elasticsearch** - опционален, приложение работает без него (fallback на БД)
4. **База данных** - обязательна для работы приложения

## 🆘 Проблемы?

1. Проверьте логи в консоли
2. Убедитесь, что все сервисы запущены (PostgreSQL, Redis, Elasticsearch)
3. Проверьте переменные окружения
4. См. раздел "Типичные проблемы" в [SETUP_GUIDE.md](./SETUP_GUIDE.md)

## ✅ Готово!

После выполнения этих шагов проект должен быть запущен и готов к использованию!

