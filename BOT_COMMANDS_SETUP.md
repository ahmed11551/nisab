# 🤖 Полная настройка команд Telegram бота

## 📋 Содержание

1. [Регистрация команд в BotFather](#1-регистрация-команд-в-botfather)
2. [Список всех команд](#2-список-всех-команд)
3. [Тестирование команд](#3-тестирование-команд)
4. [Автоматическая регистрация команд (через API)](#4-автоматическая-регистрация-команд-через-api)

---

## 1. Регистрация команд в BotFather

### Шаг 1.1: Найти @BotFather

1. Откройте Telegram
2. Найдите **@BotFather**
3. Отправьте `/mybots`
4. Выберите ваш бот **@Nisab_sad_bot**

### Шаг 1.2: Зарегистрировать команды

В @BotFather выберите "Edit Bot" → "Edit Commands" или отправьте:

```
/setcommands
```

Затем выберите ваш бот и отправьте следующий список команд:

```
start - Главное меню с основными функциями
donate - Пожертвовать в фонды
sadaqa - Пожертвовать (альтернативная команда)
support - Поддержать проект
zakat - Калькулятор закята
subscribe - Подписки на регулярные пожертвования
help - Справка по использованию бота
info - Информация о боте
```

**Или вручную через @BotFather:**

1. Откройте @BotFather
2. Отправьте `/setcommands`
3. Выберите ваш бот: **@Nisab_sad_bot**
4. Скопируйте и отправьте список команд выше

### Шаг 1.3: Проверить команды

После регистрации команд:
1. Откройте вашего бота в Telegram
2. Начните вводить `/` — должны появиться все зарегистрированные команды

---

## 2. Список всех команд

### Основные команды

| Команда | Описание | Ответ бота |
|---------|----------|------------|
| `/start` | Главное меню | Показывает главное меню с кнопками |
| `/help` | Справка | Показывает справку по использованию |
| `/info` | Информация | Показывает информацию о боте |

### Функциональные команды

| Команда | Описание | Ответ бота |
|---------|----------|------------|
| `/donate` или `/sadaqa` | Пожертвовать | Меню пожертвований |
| `/support` | Поддержать проект | Кнопки быстрой поддержки (500₽, 1000₽, 2500₽) |
| `/zakat` | Калькулятор закята | Открывает калькулятор закята |
| `/subscribe` | Подписки | Открывает меню подписок |

---

## 3. Тестирование команд

### Протестируйте каждую команду:

1. **`/start`**
   - ✅ Должно показать: "Добро пожаловать в Nisab! 🕌"
   - ✅ Должны появиться кнопки: Пожертвовать, Подписка, Калькулятор закята, Кампании, История, Помощь

2. **`/help`**
   - ✅ Должно показать: "📖 Помощь по использованию бота"
   - ✅ Должен быть список команд и функций

3. **`/info`**
   - ✅ Должно показать: "ℹ️ Информация о боте"
   - ✅ Должны быть: Название, Username, Версия

4. **`/donate`**
   - ✅ Должно показать: "Выберите действие:"
   - ✅ Должны быть кнопки: Пожертвовать, Поддержать проект

5. **`/support`**
   - ✅ Должно показать: "Поддержка проекта:"
   - ✅ Должны быть кнопки: 500 ₽, 1000 ₽, 2500 ₽

6. **`/zakat`**
   - ✅ Должно показать: "Калькулятор закята:"
   - ✅ Должна быть кнопка: "🌐 Открыть калькулятор"

7. **`/subscribe`**
   - ✅ Должно показать: "Подписки:"
   - ✅ Должна быть кнопка: "🌐 Открыть подписки"

---

## 4. Автоматическая регистрация команд (через API)

### Создать скрипт для регистрации команд

Создайте файл `backend/scripts/setCommands.ts`:

```typescript
import axios from 'axios'
import dotenv from 'dotenv'
import path from 'path'

dotenv.config({ path: path.resolve(__dirname, '../.env') })

const BOT_TOKEN = process.env.TELEGRAM_BOT_TOKEN

if (!BOT_TOKEN) {
  console.error('❌ TELEGRAM_BOT_TOKEN not found in .env')
  process.exit(1)
}

const commands = [
  { command: 'start', description: 'Главное меню с основными функциями' },
  { command: 'donate', description: 'Пожертвовать в фонды' },
  { command: 'sadaqa', description: 'Пожертвовать (альтернативная команда)' },
  { command: 'support', description: 'Поддержать проект' },
  { command: 'zakat', description: 'Калькулятор закята' },
  { command: 'subscribe', description: 'Подписки на регулярные пожертвования' },
  { command: 'help', description: 'Справка по использованию бота' },
  { command: 'info', description: 'Информация о боте' },
]

async function setCommands() {
  try {
    const response = await axios.post(
      `https://api.telegram.org/bot${BOT_TOKEN}/setMyCommands`,
      { commands }
    )

    if (response.data.ok) {
      console.log('✅ Команды успешно зарегистрированы!')
      console.log('\nЗарегистрированные команды:')
      commands.forEach((cmd) => {
        console.log(`  /${cmd.command} - ${cmd.description}`)
      })
    } else {
      console.error('❌ Ошибка при регистрации команд:', response.data)
    }
  } catch (error: any) {
    console.error('❌ Ошибка:', error.response?.data || error.message)
    process.exit(1)
  }
}

setCommands()
```

### Добавить скрипт в package.json

Добавьте в `backend/package.json`:

```json
{
  "scripts": {
    "set-commands": "ts-node scripts/setCommands.ts"
  }
}
```

### Запустить регистрацию команд

```bash
cd backend
npm run set-commands
```

---

## ✅ Чек-лист настройки команд

- [ ] Команды зарегистрированы в @BotFather через `/setcommands`
- [ ] Команда `/start` работает и показывает главное меню
- [ ] Команда `/help` работает и показывает справку
- [ ] Команда `/info` работает и показывает информацию о боте
- [ ] Команда `/donate` работает и показывает меню пожертвований
- [ ] Команда `/support` работает и показывает кнопки быстрой поддержки
- [ ] Команда `/zakat` работает и открывает калькулятор
- [ ] Команда `/subscribe` работает и открывает меню подписок
- [ ] Все кнопки работают корректно
- [ ] Mini App открывается при нажатии на кнопки

---

## 🔧 Решение проблем

### Команды не появляются в списке

1. Проверьте, что команды зарегистрированы через `/setcommands`
2. Попробуйте перезапустить Telegram
3. Проверьте, что вы используете правильного бота

### Команды не работают

1. Проверьте, что backend запущен
2. Проверьте, что webhook настроен
3. Проверьте логи backend на ошибки

### Кнопки не работают

1. Проверьте, что `CORS_ORIGIN` правильно настроен в `.env`
2. Проверьте, что Mini App URL правильный
3. Проверьте логи backend

---

## 📚 Дополнительная информация

- [Telegram Bot Commands Documentation](https://core.telegram.org/bots/api#setmycommands)
- [Telegram Bot Menu Button Documentation](https://core.telegram.org/bots/api#setchatmenubutton)

