# ⚡ Быстрое решение проблемы подключения

## 🔍 Проблема: "Не удалось подключиться к серверу"

Это означает, что **frontend не может подключиться к backend**.

## ✅ Быстрое решение (3 шага)

### Шаг 1: Убедиться, что backend запущен

Откройте **новый терминал** и запустите:

```bash
cd C:\Users\Dev-Ops\Desktop\nisab\backend
npm run dev
```

**Должно вывести:**
```
Server running on port 3000
✓ Database connection: OK
✓ Telegram Bot Token: OK
```

### Шаг 2: Убедиться, что frontend запущен

Откройте **другой терминал** и запустите:

```bash
cd C:\Users\Dev-Ops\Desktop\nisab\frontend
npm run dev
```

**Должно вывести:**
```
VITE v5.x.x  ready in xxx ms
➜  Local:   http://localhost:5173/
```

### Шаг 3: Проверить в браузере

1. Откройте http://localhost:5173
2. Откройте DevTools (F12)
3. Перейдите на вкладку **Console**
4. Проверьте, есть ли ошибки

---

## ⚠️ Важно!

1. **Оба сервера должны быть запущены одновременно:**
   - Backend на порту **3000**
   - Frontend на порту **5173**

2. **Если меняли .env файлы, перезапустите серверы!**
   - Vite не подхватывает изменения .env без перезапуска

3. **Проверьте файлы .env:**

   **backend/.env должен содержать:**
   ```env
   CORS_ORIGIN=http://localhost:5173
   FRONTEND_URL=http://localhost:5173
   ```

   **frontend/.env должен содержать:**
   ```env
   VITE_API_URL=http://localhost:3000
   ```

---

## 🔍 Если не помогает

1. **Остановите все процессы node:**
   ```powershell
   Get-Process -Name node | Stop-Process -Force
   ```

2. **Запустите заново:**
   - Терминал 1: `cd backend && npm run dev`
   - Терминал 2: `cd frontend && npm run dev`

3. **Откройте браузер:** http://localhost:5173

4. **Проверьте консоль браузера (F12):**
   - Должны быть ошибки с подробностями

---

## 📞 Если все еще не работает

Проверьте:
1. ✅ База данных PostgreSQL запущена?
2. ✅ Порт 3000 свободен?
3. ✅ Порт 5173 свободен?
4. ✅ Файлы .env на месте и правильные?

Полная инструкция: [TROUBLESHOOTING_CONNECTION.md](./TROUBLESHOOTING_CONNECTION.md)

