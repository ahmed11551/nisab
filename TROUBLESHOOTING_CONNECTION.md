# 🔧 Решение проблемы "Не удалось подключиться к серверу"

## ✅ Проверка 1: Backend запущен

### Проверить статус backend:

```bash
curl http://localhost:3000/health
```

**Должен вернуться:**
```json
{"status":"ok","timestamp":"..."}
```

### Если не работает:

1. **Запустить backend:**
   ```bash
   cd backend
   npm run dev
   ```

2. **Проверить, что порт 3000 свободен:**
   ```bash
   # Windows PowerShell
   Get-NetTCPConnection -LocalPort 3000
   ```

---

## ✅ Проверка 2: Frontend настроен правильно

### Проверить frontend/.env:

```bash
cd frontend
cat .env
```

**Должен содержать:**
```env
VITE_API_URL=http://localhost:3000
VITE_APP_NAME=Nisab
VITE_TELEGRAM_BOT_NAME=Nisab_sad_bot
```

### Если нет файла или неправильный URL:

1. **Создать/обновить frontend/.env:**
   ```env
   VITE_API_URL=http://localhost:3000
   VITE_APP_NAME=Nisab
   VITE_TELEGRAM_BOT_NAME=Nisab_sad_bot
   ```

2. **ВАЖНО: Перезапустить frontend после изменения .env!**
   ```bash
   # Остановить frontend (Ctrl+C)
   # Затем запустить снова:
   cd frontend
   npm run dev
   ```

---

## ✅ Проверка 3: CORS настроен правильно

### Проверить backend/.env:

```bash
cd backend
cat .env
```

**Должен содержать:**
```env
CORS_ORIGIN=http://localhost:5173
FRONTEND_URL=http://localhost:5173
```

### Если неправильно настроен:

1. **Обновить backend/.env:**
   ```env
   CORS_ORIGIN=http://localhost:5173
   FRONTEND_URL=http://localhost:5173
   ```

2. **Перезапустить backend:**
   ```bash
   # Остановить backend (Ctrl+C)
   # Затем запустить снова:
   cd backend
   npm run dev
   ```

---

## ✅ Проверка 4: Оба сервера запущены

### Проверить оба порта:

```bash
# Проверить backend (порт 3000)
curl http://localhost:3000/health

# Проверить frontend (порт 5173)
curl http://localhost:5173
```

### Если не работают:

1. **Запустить backend:**
   ```bash
   cd backend
   npm run dev
   ```

2. **Запустить frontend (в другом терминале):**
   ```bash
   cd frontend
   npm run dev
   ```

---

## 🔍 Диагностика в браузере

### Открыть консоль разработчика (F12):

1. Перейдите на http://localhost:5173
2. Откройте DevTools (F12)
3. Перейдите на вкладку **Console**
4. Перейдите на вкладку **Network**

### Проверить ошибки:

1. **CORS ошибка:**
   ```
   Access to fetch at 'http://localhost:3000/api/v1/...' from origin 'http://localhost:5173' has been blocked by CORS policy
   ```
   **Решение:** Проверить CORS_ORIGIN в backend/.env

2. **Connection refused:**
   ```
   Failed to fetch
   ERR_CONNECTION_REFUSED
   ```
   **Решение:** Backend не запущен или на другом порту

3. **404 ошибка:**
   ```
   404 Not Found
   ```
   **Решение:** Проверить URL в frontend/.env (должен быть http://localhost:3000)

---

## 🚀 Быстрый способ исправить

### 1. Остановить все процессы:

```bash
# Windows PowerShell
Get-Process -Name node | Stop-Process -Force
```

### 2. Проверить файлы .env:

**backend/.env:**
```env
CORS_ORIGIN=http://localhost:5173
FRONTEND_URL=http://localhost:5173
```

**frontend/.env:**
```env
VITE_API_URL=http://localhost:3000
```

### 3. Запустить backend (Терминал 1):

```bash
cd backend
npm run dev
```

### 4. Запустить frontend (Терминал 2):

```bash
cd frontend
npm run dev
```

### 5. Проверить в браузере:

Откройте http://localhost:5173 и проверьте работу приложения.

---

## ⚠️ Важные замечания

1. **Vite требует перезапуска** после изменения .env файла
2. **Переменные окружения** должны начинаться с `VITE_` для frontend
3. **CORS** должен разрешать запросы с frontend URL
4. **Оба сервера** должны быть запущены одновременно

---

## 📞 Если не помогло

1. Проверьте логи backend в консоли
2. Проверьте логи frontend в консоли
3. Проверьте консоль браузера (F12 → Console)
4. Проверьте Network tab в DevTools для деталей ошибок

