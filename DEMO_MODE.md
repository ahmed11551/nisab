# 📦 Демо-режим приложения

## Что такое демо-режим?

Демо-режим позволяет работать с приложением **без запуска бэкенда**. Все API вызовы автоматически перенаправляются на моки, которые возвращают реалистичные демо-данные.

## Как включить демо-режим?

### Автоматическое включение (по умолчанию)

Демо-режим **автоматически активируется** при сетевых ошибках, если:
- API сервер недоступен (ошибка сети)
- API возвращает ошибку подключения
- Приложение работает в режиме разработки без запущенного бэкенда

### Ручное включение через .env

Создайте или отредактируйте файл `frontend/.env`:

```env
# Включить демо-режим принудительно
VITE_DEMO_MODE=true

# Или установить URL на "demo"
VITE_API_URL=demo
```

## Что работает в демо-режиме?

### ✅ Полностью работающие разделы:

1. **Пожертвовать** (`/donate`)
   - ✅ Список фондов (5 демо-фондов)
   - ✅ Фильтры по стране и категории
   - ✅ Поиск фондов
   - ✅ Выбор суммы и пожертвование (имитация)

2. **Поддержать** (`/support`)
   - ✅ Быстрое пожертвование (имитация)

3. **Целевые кампании** (`/campaigns`)
   - ✅ Список кампаний (5 демо-кампаний)
   - ✅ Фильтры по стране и категории
   - ✅ Просмотр деталей кампании
   - ✅ Создание новой кампании (имитация)
   - ✅ Пожертвование в кампанию (имитация)

4. **Подписка** (`/subscription`)
   - ✅ Просмотр тарифов
   - ✅ Оформление подписки (имитация)

5. **Калькулятор закята** (`/zakat`)
   - ✅ Расчет закята (полный функционал)
   - ✅ Выплата закята (имитация)

6. **Фонды-партнёры** (`/partners`)
   - ✅ Список партнёров (4 демо-партнёра)
   - ✅ Фильтры по стране и категориям
   - ✅ Заявка на партнёрство (имитация)

7. **История** (`/history`)
   - ✅ История пожертвований (5 демо-записей)
   - ✅ Фильтры по типу и периоду
   - ✅ Отчёты фондов (2 демо-отчёта)

8. **Главная страница** (`/`)
   - ✅ Статистика (количество фондов и кампаний)
   - ✅ Быстрый доступ ко всем разделам

## Демо-данные

### Фонды (5 шт.)
- Фонд помощи нуждающимся (Россия)
- Образовательный фонд (Казахстан)
- Фонд мечетей (Россия)
- Международный фонд помощи (Турция)
- Фонд поддержки семей (Россия)

### Кампании (5 шт.)
- Ремонт мечети в Казани (50% собрано)
- Поддержка детей-сирот (65% собрано)
- Образовательные курсы (64% собрано)
- Помощь беженцам из Сирии (62% собрано)
- Строительство новой мечети (42% собрано)

### Партнёры (4 шт.)
- Фонд помощи нуждающимся
- Образовательный фонд
- Фонд мечетей
- Международный фонд помощи

### История (5 записей)
- Пожертвования в фонды
- Подписки
- Закят
- Пожертвования в кампании

### Отчёты (2 шт.)
- Отчёты фондов с суммами

## Что НЕ работает в демо-режиме?

### ⚠️ Имитация (возвращают успешный ответ, но не выполняют реальное действие):

- ❌ Реальные платежи через YooKassa/CloudPayments
- ❌ Сохранение данных в базе данных
- ❌ Создание реальных кампаний
- ❌ Реальная отправка заявок на партнёрство
- ❌ Реальные подписки

**Примечание:** Все формы работают, валидация работает, но данные не сохраняются в реальную базу данных.

## Как проверить демо-режим?

1. **Запустите только фронтенд** (без бэкенда):
   ```bash
   cd frontend
   npm run dev
   ```

2. **Откройте консоль браузера** - вы увидите:
   ```
   📦 ДЕМО-РЕЖИМ АКТИВЕН
   Приложение работает с демо-данными.
   ```

3. **Проверьте функционал:**
   - Перейдите по всем разделам
   - Попробуйте фильтры
   - Создайте кампанию
   - Сделайте пожертвование
   - Посмотрите историю

## Структура демо-данных

```
frontend/src/
├── data/
│   └── demoData.ts          # Все демо-данные
├── services/
│   ├── api.ts               # Переключатель API/Мок
│   └── mockApi.ts           # Моки API (имитация реального API)
```

## Имитация работы API

Моки полностью имитируют работу реального API:
- ✅ Задержка ответа (~300ms)
- ✅ Правильная структура данных
- ✅ Фильтрация и пагинация
- ✅ Поиск
- ✅ Валидация параметров

## Примеры использования

### Просмотр фондов:
```typescript
// Автоматически использует моки если API недоступен
const funds = await fundsApi.list({ country: 'RU' })
```

### Создание кампании:
```typescript
// В демо-режиме возвращает успешный ответ с ID кампании
const campaign = await campaignsApi.create({
  title: 'Новая кампания',
  description: 'Описание',
  category: 'mosque',
  goal_amount: 100000,
})
```

### Пожертвование:
```typescript
// В демо-режиме возвращает URL для оплаты (демо)
const donation = await donationsApi.init({
  fund_id: 'demo-fund-1',
  amount: { value: 1000, currency: 'RUB' },
})
```

## Переключение между режимами

### Демо → Реальный API:
1. Уберите `VITE_DEMO_MODE=true` из `.env`
2. Убедитесь, что бэкенд запущен
3. Перезапустите фронтенд

### Реальный API → Демо:
1. Остановите бэкенд
2. Или добавьте `VITE_DEMO_MODE=true` в `.env`
3. Приложение автоматически переключится на демо

## Важные замечания

1. **Демо-данные НЕ сохраняются** - при обновлении страницы данные восстанавливаются к исходным
2. **Платежи НЕ обрабатываются** - возвращается демо-URL оплаты
3. **Созданные кампании** - временные, при обновлении исчезают
4. **Все операции** - полностью имитируются для демонстрации функционала

## Для клиента

**Клиент может:**
- ✅ Просматривать все разделы без запуска сервера
- ✅ Тестировать весь функционал из ТЗ
- ✅ Видеть как будут выглядеть карточки с данными
- ✅ Проверять фильтры, поиск, пагинацию
- ✅ Создавать кампании (демо)
- ✅ Делать пожертвования (демо)
- ✅ Использовать калькулятор закята (полностью работает)

**Клиент НЕ может:**
- ❌ Выполнять реальные платежи
- ❌ Сохранять данные в базу
- ❌ Получать реальные отчёты

---

**Статус:** ✅ Демо-режим полностью реализован и готов к использованию!

